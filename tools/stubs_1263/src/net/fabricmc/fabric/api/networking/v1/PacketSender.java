package net.fabricmc.fabric.api.networking.v1;

/**
 * Compile-only stub of Fabric API's {@code PacketSender}. The mod never calls
 * methods on it; only the type is needed for the {@code JOIN} lambda.
 */
public interface PacketSender {
}
